# -------------------------------------------------------
# SSH and remote connection aliases
# -------------------------------------------------------

# Cluster connections - using SSH config file
alias cluster1="ssh cluster1"
alias Xcluster1="ssh Xcluster1"
alias cluster2="ssh cluster2"
alias Xcluster2="ssh Xcluster2"

# QUARKS connection
alias quarks="ssh quarks"

# NEUTRON connection
alias neutron="ssh neutron"

# JUWELS connections
alias goJUWELS="ssh juwels"
alias goJUWELS_office="ssh juwels-office"
alias goJUWELS_new_office="ssh juwels-new-office"
alias goJUWELS_home_WLAN="ssh juwels-home"
